{% extends 'banner.html.twig' %}
{% block content %}
    <div id="page-content-div">
        <form action="{{ action }}" method ="{{ method }}">
            <h3>{{ page_heading_2 }}</h3>
            <fieldset>
                <p>{{ page_text }}</p>

                <label for="typename">System Type Name:</label> <input id="typename" type="text" name="typename" size="10" value="{{ type.systemtypename }}" required>
                <br>
                <label for="typedesc">System Type Description:</label> <input id="typedesc" type="text" name="typedesc" size="10" value="{{ type.systemtypedesc }}" required>
                <hr>
                <input type="submit" value="edit">
            </fieldset>
        </form>
    </div>
{% endblock %}